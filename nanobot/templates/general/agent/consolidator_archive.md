Extract key facts from this conversation. Only output items matching these categories:
- User facts
- Decisions
- Solutions
- Events
- Preferences

Priority: user corrections and preferences, then working solutions, then decisions and events.

Skip code patterns derivable from source, git history, and anything already captured in memory.

Output concise bullet points, one fact per line.
If nothing noteworthy happened, output: (nothing)
