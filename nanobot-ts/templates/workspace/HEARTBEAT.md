# Heartbeat

List background tasks that nanobot should periodically review here.

Use short plain-language instructions. Remove items when they are no longer active.

Example:

- Check whether staging deploys are failing and notify me only if there is something actionable.

